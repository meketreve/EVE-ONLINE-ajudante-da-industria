{
  "generated_at": "2026-04-21T14:27:14.141Z",
  "achievements": [
    "NiceGUI migration landed (commit ca75d33) — replaced Jinja/HTMX stack",
    "Backlog items 2-12 shipped (commit 63164b2)",
    "Reprocessing module added (models/reprocessing.py + ui/reprocessing_page.py)",
    "Ranking UI split into list + item detail pages (ranking_page.py, ranking_item_page.py)",
    "Recursive BOM calc live (blueprint_service.get_recursive_bom, aggregate_bom_leaves)",
    "Cache-first market flow (market_service.get_prices_cache_only + background refresh)",
    "Structure discovery + crawler pipeline operational (3 discovery / 2 crawl workers)"
  ],
  "improvements": [
    "industry_page.py at ~12k tok and ranking_page.py at ~11k tok — split into sub-components to cut reload cost",
    "settings_page.py ~12k tok — extract sections (freight, structure bonuses, tax) into components/",
    "Migrations embedded in database.py create_tables() — move to Alembic before schema churn bites",
    "esi_client.py has 18 methods — split into character/market/structure clients",
    "scripts/atualizar_precos_mercado.py and crawler_service.py duplicate ESI crawl logic — consolidate",
    "No pytest files visible in anatomy — add service-layer tests for industry_calculator + blueprint_service recursion",
    "database.db-shm tracked (8.7k tok) — add *.db-shm / *.db-wal to .gitignore",
    "Memory log empty across 4 sessions — hook not appending, fix OpenWolf logging"
  ],
  "next_tasks": [
    "Finish import_ranking endpoint: wire freight_cost_per_m3 × volume into margin formula (api/industry.py + models/user_settings.py already staged)",
    "Per-BOM-item ME override (todo item 3) — extend blueprint_service to accept ME map, UI input in bom_tree.py",
    "Flag-to-buy leaf items in BOM (todo item 5) — toggle in bom_tree, skip manufacture branch",
    "Station rig auto-detect from character assets (todo item 9) — extend discovery_service to scan rigs, apply bonuses in structure_selector",
    "Market volume graph (todo item 11) — price_chart.py already exists, add snapshot history query in market_service",
    "Commit the 40+ dirty files — branch risk growing, review staged changes vs WIP"
  ],
  "risks": [
    "40+ uncommitted modified files on main — merge conflict + review-at-scale risk, no bisect if regression hits",
    ".wolf/ and mess/ untracked, .claude/settings.json not in .gitignore audit — may leak local config",
    "OAuth tokens in characters table — verify encryption at rest, SQLite file is plaintext",
    "Scheduler (15min recrawl / 1h clean / 6h rediscovery) runs in-process — single uvicorn restart loses job queue state",
    "ESI rate limits not visible in esi_client.py summary — bulk crawl + discovery could trip error limit",
    "SDE import is manual one-shot — no version tracking, stale data when CCP patches",
    "create_tables() migrations are append-only ALTER blocks — no rollback, no version table, destructive-change hazard",
    "SQLite WAL + multi-worker crawl — concurrent writes can starve under high order volume"
  ]
}